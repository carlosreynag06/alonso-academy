# Route and State Matrix

## Scope

This matrix inventories every current application page, API route, and global route surface in the repository. "Owner" identifies the present product actor or system boundary. "Disposition" states whether the current path or concept is retained, replaced, or removed. "Recovery owner" identifies the phase responsible for the target behavior.

No row in this document is evidence that the corresponding product outcome is ready.

**Recovery 1 handoff (2026-07-12):** the behavior below reflects the committed Recovery 1 repository contract. It does not establish that migration `20260712120000_recovery_1_authoritative_domain.sql` is applied to hosted Supabase or that any route is verified against the real family identities.

## Application routes

| Route or surface | Current owner | Current behavior and material states | Disposition | Recovery owner |
| --- | --- | --- | --- | --- |
| `/` | Public authentication | Renders the same `LoginScreen` as `/login`; it does not redirect an existing session. | Remove the duplicate surface and redirect to canonical `/login`. | Recovery 4 and Recovery 5. |
| `/login` | Public authentication | Direct password form for the two configured identities. Error query states: `configuration`, `invalid`, `service`, `forbidden`. Successful identity redirects to `/parent` or `/alonso`. | Keep the path and direct-password policy; replace presentation and make signed-in destination handling authoritative. | Recovery 4 and Recovery 5. |
| `/parent/login` | Legacy authentication | Redirects to `/login`. | Remove after a compatibility redirect period. | Recovery 4. |
| `/auth/confirm` | Legacy authentication | Redirects to `/login`; no token exchange occurs. | Remove. Magic-link authentication is excluded. | Recovery 4. |
| `/dev/fixtures` | Recovery infrastructure | Local-only scenario controller backed by an opaque cookie and ignored JSON state. It returns 404 unless development, an explicit fixture flag, and loopback host checks all pass. | Retain through recovery; remove or disable permanently before any production deployment. | Recovery 0 through Recovery 8. |
| `/parent` | Parent | Derives the next action from one unit, the latest approved weekly plan, approved lesson-like artifact counts, and validated/failed artifacts. Its readiness projection is not the authoritative five-slot publication board. | Keep the path; replace the view, query model, and readiness calculation. | Recovery 4. |
| `/parent/recovery` | Parent recovery | Authenticated truth surface showing the Recovery 1 `implemented` repository status, hosted counts/configuration when available, or explicitly labeled local fixture state. Generation, curriculum decisions, and live speech remain locked; audited publication/attempt transitions are the narrow enabled boundary. | Retain as an operational recovery surface until final pilot acceptance; reassess in Recovery 8. | Recovery 0, Recovery 1, and Recovery 8. |
| `/parent/curriculum` | Parent | Displays six phase records and all units, with Phase A described as active regardless of a complete pilot. | Keep the path; replace with a versioned adult curriculum workbench. | Recovery 4. |
| `/parent/curriculum/[unitId]` | Parent | Displays current vocabulary, frames, phonics, and writing rows. Curriculum decisions are locked during Recovery 1; the old whole-unit mutation is not the target revision workflow. | Keep the path; replace the whole-unit mutation with immutable revisions and target-level decisions. | Recovery 4. |
| `/parent/generation` | Parent | Generation is locked. The transitional Recovery 1 view reads one authoritative current learning week, renders all five exact day slots with artifact/assignment state, and retains immutable artifact history links. It does not provide the final week/review information architecture. | Replace with a five-day `/parent/week` production board and a separate `/parent/review` queue; keep a compatibility redirect if useful. | Recovery 1 implemented the transitional publication view; Recovery 3 and Recovery 4 own the final product. |
| `/parent/artifacts/[artifactId]` | Parent | Displays the existing partial artifact/validation view plus private approval and state-gated Recovery 1 controls for week creation, scheduling, publication, replay permission, replacement, withdrawal, approval revocation, and archival. Every mutation requires an authenticated parent and a note/reason; unavailable transitions show prerequisites. Query state also includes sanitized publication success/error. | Keep the stable artifact URL; replace the incomplete preview renderer and rejected adult composition while retaining the authoritative lifecycle actions. | Recovery 1 implemented lifecycle control; Recovery 3 and Recovery 4 own complete preview and final UI. |
| `/alonso` | Child | Calls the assignment-bound child-home RPC. It renders at most one currently available `published` mission or a closed state; artifact approval alone cannot satisfy the query. The payload can contain completed replay assignments, but the current page does not surface replay/history/retrieval controls. The CSS-only child composition remains rejected. | Keep the path; rebuild as the approved character world with one mission, replay/review entry, and picture collection. | Recovery 1 implemented delivery authority; Recovery 2 and Recovery 5 own the product experience. |
| `/alonso/lesson/[attemptId]` | Child | Loads only an assignment-authorized, approved, exactly bound, runtime-ready attempt payload and hydrates the complete authoritative snapshot into the v1 renderer. Unsupported, stale, or unavailable attempts fail closed. The renderer remains text-dependent and is not the final child product. | Keep the path; replace v1 presentation with assignment-bound schema-v2 playback while retaining server authority and resume. | Recovery 1 implemented runtime authority; Recovery 3 and Recovery 5 own the final contract/player. |
| `/favicon.ico` | Global shell | Static application icon supplied by the app directory. | Keep path; replace artwork only after the approved identity direction. | Recovery 2 and Recovery 5. |

## API routes

| Route | Method | Current owner | Current behavior and material states | Disposition | Recovery owner |
| --- | --- | --- | --- | --- | --- |
| `/api/child/attempts/[attemptId]/audio/[blockId]` | GET | Child audio | Authorizes the child through the assignment-bound attempt payload, extracts only child-safe supported block text, and returns fixture audio or cached/on-demand ElevenLabs MP3. Returns `401`, `404`, `423`, or `503` failure states. On-demand audio is not pre-publication readiness. | Replace with an approved asset registry and audio pre-generated before publication. Retain assignment authorization and server-only delivery. | Recovery 1 hardened authorization; Recovery 3 and Recovery 6 own production assets/audio. |
| `/api/child/attempts/[attemptId]/speech/[blockId]` | POST | Child speech | Requires child auth plus bounded audio and idempotency/version fields. Hosted live speech is deliberately locked and returns recoverable `503`; fixtures can simulate a derived speech outcome locally. The service-role-only provider evidence RPC is not exposed through this browser route. | Keep the ephemeral server-provider boundary; activate only with Recovery 6 rate limits, approved voice, cleanup proof, and provider recovery. | Recovery 1 implemented the fail-closed boundary; Recovery 6 owns activation. |
| `/api/child/attempts/[attemptId]/evidence` | POST | Child evidence | Accepts only a UUID client event, expected state version, current block ID, semantic choice/acknowledgement response, and bounded latency. Calls `command_child_attempt(..., 'submit_response', ...)`; the server derives correctness, first attempt, support, retries, authority, evidence, feedback, and progression eligibility. Stale state returns `409`. | Retain as a compatibility HTTP boundary over the authoritative command; align with schema-v2 response semantics later. | Recovery 1 implemented; Recovery 3 and Recovery 5 extend supported activities. |
| `/api/child/attempts/[attemptId]/progress` | POST | Child attempt state | Accepts an idempotent client event, expected state version, current block ID, and one allowed command. Calls `command_child_attempt`; the server owns pause/resume, breaks, listening, hints, retry, acknowledgement, and ordered advance. Stale state returns `409`. | Retain the narrow command boundary and update presentation callers during the player rebuild. | Recovery 1 implemented; Recovery 5 owns the final player. |
| `/api/child/attempts/[attemptId]/complete` | POST | Child completion | Accepts only client event, expected version, and block ID, then delegates to `command_child_attempt(..., 'complete', ...)`. The server requires every required runtime block and qualifying server/provider-derived exit evidence. Stale state returns `409`. | Retain the authoritative completion command; extend only through reviewed schema-v2 requirements. | Recovery 1 implemented; Recovery 3, Recovery 5, and Recovery 8 verify the final gate. |

## Global route boundaries

| Surface | Current owner | Current behavior | Disposition | Recovery owner |
| --- | --- | --- | --- | --- |
| Root `layout.tsx` | Global shell | Defines application metadata, fonts, body, and shared global CSS. | Keep the boundary; replace identity and visual tokens after art-direction approval. | Recovery 2, Recovery 4, and Recovery 5. |
| Root `loading.tsx` | Global shell | Generic academy loading state. | Keep the boundary; replace visual/copy treatment. | Recovery 4 and Recovery 5. |
| Root `error.tsx` | Global shell | Generic retry surface. | Keep the retry boundary; replace presentation and ensure errors do not imply successful mutations. | Recovery 4 and Recovery 5. |
| Root `not-found.tsx` | Global shell | Generic return-home state. | Keep behavior; replace presentation and role-safe destination. | Recovery 4 and Recovery 5. |
| Parent `loading.tsx` | Parent shell | Wraps a generic loading component in the parent shell. | Keep boundary; replace the rejected shell and loading composition. | Recovery 4. |
| Parent `error.tsx` | Parent shell | Retry state claiming no curriculum or approval changed. | Keep boundary; replace shell and use evidence-based mutation messaging. | Recovery 4. |
| Alonso `loading.tsx` | Child shell | Generic child loading state. | Keep boundary; replace with character-led, audio-safe loading. | Recovery 5. |
| Alonso `error.tsx` | Child shell | Child retry state with ask-a-parent fallback. | Keep boundary; replace with character-led recovery and a protected parent exit. | Recovery 5. |
| `proxy.ts` | Authentication infrastructure | Refreshes Supabase sessions for root, login, parent, child, and auth paths. | Keep and audit matcher/session behavior. | Recovery 1, Recovery 4, and Recovery 5. |

## Material state ownership

| State family | Current states | Current transition owner | Current limit or defect | Disposition | Recovery owner |
| --- | --- | --- | --- | --- | --- |
| Parent/child access | `configuration_required`, `signed_out`, `forbidden`, `ready` | Server auth helpers plus Supabase claims/profile RPCs | Direct family password roles remain; protected child-to-parent exit design is not implemented. | Retain server authorization; expose role-safe recovery in rebuilt interfaces. | Recovery 4 and Recovery 5. |
| Generation readiness | provider configured/unconfigured; curriculum approved/unapproved; ready/blocked | Server environment plus unit state | Generation is locked; provider configuration is not operational health. | Replace with truthful blocker/readiness projection. | Recovery 3 and Recovery 4. |
| Curriculum status | `draft`, `approved`, `inactive`, `archived` | Existing whole-unit RPC; decisions locked in Recovery 1 | Immutable revisions, target-level decisions, and complete revoke/archive ownership remain absent. | Replace lifecycle with immutable revisions and explicit decisions. | Recovery 4. |
| Artifact status | `draft`, `validating`, `validation_failed`, `validated`, `approved`, `active`, `completed`, `archived` | Generation pipeline plus audited approval/revoke/archive RPCs | Recovery 1 makes approval private, but v1 generation/validation and preview remain incomplete. | Retain immutable artifact decisions; schema-v2 validation and UI remain later work. | Recovery 1 implemented lifecycle separation; Recovery 3 and Recovery 4 extend it. |
| Artifact binding | slot ID; `not_applicable`, `unverified`, `valid`, `invalid`; runtime-ready flag | Recovery 1 binding/runtime functions and artifact trigger | V1 registry supports daily/review lessons only and is not the complete oral-first schema. | Retain exact binding; replace runtime preparation with schema v2. | Recovery 1 implemented; Recovery 3 extends. |
| Artifact lineage | version, previous version, lineage key, parent artifact, exact slot/day/kind | Generation plus Recovery 1 foreign keys/immutability | Regeneration remains locked; comparison and complete preview are absent. | Retain immutable lineage and exact binding. | Recovery 1 and Recovery 3. |
| Learning week | `planned`, `active`, `completed`, `archived`; exactly five slots | Parent-only Recovery 1 RPCs and deferred database constraints | Final week workbench and five-lesson readiness projection are not implemented. | Retain authoritative week/slot model. | Recovery 1 implemented; Recovery 4 presents it. |
| Lesson assignment | `assigned`, `scheduled`, `published`, `completed`, `withdrawn`, `replaced`, `archived`, `quarantined` | Parent-only Recovery 1 RPCs | One active assignment per slot and one published mission per child are enforced; hosted behavior is unverified. | Retain authoritative assignment lifecycle. | Recovery 1 implemented; Recovery 4 presents it. |
| Parent approval action | `approved`, `rejected`, `revoked` | Audited artifact RPCs | Approval is private and revocable; request-change/version comparison remain incomplete. | Retain audited decisions; complete review workflow later. | Recovery 1 and Recovery 4. |
| Lesson attempt | `in_progress`, `paused`, `completed`, `abandoned`; modes `learning`, `replay`, `scheduled_retrieval` | `start_child_assignment` and `command_child_attempt` | Assignment/mode ownership and resume snapshots exist; final player and observed recovery are unverified. | Retain server-authoritative attempt model. | Recovery 1 implemented; Recovery 5 presents it. |
| Player progression | current block, block states, state version, command events | `command_child_attempt` | Browser no longer owns order/state, but only v1 block types are prepared. | Retain command/version/idempotency model and extend for schema v2. | Recovery 1 and Recovery 3. |
| Activity evidence | response provenance, first attempt, support, correctness, retries, latency, authority, rule version, completion qualification | Recovery 1 server scorer or service-role provider result | Legacy evidence is quarantined; live provider evidence is locked; mastery does not consume this history yet. | Retain provenance/authority and extend reviewed rubrics. | Recovery 1, Recovery 3, Recovery 6, and Recovery 7. |
| Support level | `independent`, `replay`, `prompted`, `reduced_choices`, `modeled` | Server-held listen/response/hint history | V1 derivation exists; final hint ladder and instructional semantics remain Recovery 3/5 work. | Retain server derivation. | Recovery 1, Recovery 3, and Recovery 5. |
| Speech outcome | `transcribed`, `silence` provider input; derived correct/incorrect/silence | Service-role-only provider RPC | Browser live speech route is locked; no approved voice, rate limits, cleanup proof, or operational fallback verification exists. | Keep fail-closed until Recovery 6. | Recovery 6. |
| Mastery stage | provisional stage vocabulary | No transition owner | Table/type existence is the only implementation. | Replace with deterministic evidence rules. | Recovery 7. |
| Review schedule | due time, priority 1–5, reason | No transition owner | Child-home retrieval list is intentionally empty; no deterministic scheduler exists. | Implement from deterministic mastery evidence. | Recovery 7. |
| Child-session token | active/revoked/expired represented by timestamps | No current application owner | Table/helper remain unused because the child signs in directly. | Remove or explicitly repurpose through an approved design. | Recovery 5. |
| Provider metadata | provider, operation, unconstrained status, safe metadata | OpenAI pipeline and service-role speech function | No operational health model or verified live speech exists. | Retain audit data; add constrained readiness projection. | Recovery 4 and Recovery 6. |
| Recovery capability | `scaffolded`, `blocked`, `implemented`, `verified`, `pilot-ready` | Recovery documentation and server status projection | Must not be inferred from a file, migration, build, or provider key alone. | Retain exactly as defined in `RECOVERY_0_BASELINE.md`. | All recoveries; final authority Recovery 8. |

## Target route ownership after recovery

The recovery plan adds parent routes that do not yet exist: Week, Review queue, Alonso operations, Progress, History, and Settings. Their absence is intentional evidence of incompleteness, not a reason to overload current routes with misleading labels. Recovery 4 owns their information architecture. Recovery 7 owns the actual progress/history/mastery data they consume.

The Recovery 1 child route contract is one currently available published assignment for the child, plus explicitly enabled completed replay assignments. Artifact approval alone never satisfies a child query. Scheduled retrieval is represented in the domain but has no recommendation/list delivery implementation yet.
